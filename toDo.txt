
Main task by subtasks:

1. Create a web application with ReactJS, web application should:
    [x] Be one-page web application
    [] Have a clear, comprehensible and responsive layout
    [x] Implement unsplash.com image retrieval API
    [x] User should be able to search images by entered keywords

2. Image app functionality:
    [x] Web application should provide user with keyword (search) input field
    [x] When keyword is entered user should be able to press search button to retrieve the images
    [x] When images are returned app should parse API response and display images in the grid below the search input field
    [x] Application should show some sort of progress (loading) indicator while images are being fetched
    [x] If there are no images by entered keyword user should see message with relevant information (it could be modal popup or text element in empty image grid)

3. Bonus tasks:
    [x] Allow user to query images by more than one keyword
    [] Host your application in GitHub Pages
    [x] Use SASS/SCSS to upstyle your web application
    [] Write tests for chosen ReactJS component
    [] Allow user to save it’s query in Redux state and list saved queries, so that when pressed that query is repeated again and user gets pictures by saved query

4. Project requisites:
    [] Project source code should be stored in GitHub, bitbucket or any other version control platform
    [] Project should have a comprehensible README file
    [] Project directory should be logically structured
    [] Source code should be clean and formatted accordingly
    [] Extensive comments in source code are not desirable, source code should be self explanatory
    [] Please follow API guidelines here